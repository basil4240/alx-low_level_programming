#include <stdlib.h>

/**
 * _abs - computes the absolute value of an n.
 * @n: The integer which absolute value is to be computed
 *
 * Return: The absolute value of n as int.
 */
int _abs(int n)
{
	int abs_value;

	abs_value = abs(n);

	return (abs_value);
}
