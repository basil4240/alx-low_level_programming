/**
 * add - adds two integers a and b and returns the result
 * @a: First number to add
 * @b: Second number
 *
 * Return: sum of a and b
 */
int add(int a, int b)
{
	int sum = a + b;

	return (sum);
}
